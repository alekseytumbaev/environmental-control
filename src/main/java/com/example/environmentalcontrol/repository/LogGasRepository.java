package com.example.environmentalcontrol.repository;

import com.example.environmentalcontrol.entity.LogGas;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogGasRepository extends JpaRepository<LogGas, Long> {
}
